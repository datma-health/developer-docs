# External Docs Folder

Store documentation imported from private repositories in subfolders.

Suggested convention:

- `external/<repo-name>/index.md` as the landing page for that repo's docs.
- Keep related pages next to that `index.md`.